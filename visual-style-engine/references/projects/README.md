# Project References — 项目参考图索引（project scope）
schema: reference_index
scope: project
version: "1.0.0"

# 项目级参考图与公共库分离：references/projects/<PROJECT_ID>/REF_PROJECT_<NN>.<ext>
# 元数据模板见 schemas/reference.schema.yaml

# 目录约定：
# references/projects/<PROJECT_ID>/
# ├── REF_PROJECT_01.<ext>
# ├── REF_PROJECT_02.<ext>
# └── references.yaml

# 角色（第 31.2 节）：overall_style / line / palette / texture / composition / character
# 项目参考可包含"人工批准的生成结果"——经 Style Learning 沉淀（第 72 节），
# 逐步优化 Adapter 与 Evaluation threshold，但不直接自动修改 Canonical Style。

# 项目 Identity 模板：schemas/identity.schema.yaml
