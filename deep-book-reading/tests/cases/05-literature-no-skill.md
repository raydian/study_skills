# Case 05 — Literature should not be forced into a method Skill

A novel has themes, character arcs, imagery, and unreliable narration but no stable procedure.

Pass only if the literary profile is used for reading and synthesis while the Skill candidate evaluator returns `reject` or `hold`; no pseudo-method is manufactured.
