# image-gen-multi 环境变量模板

复制为本地 .env 或写入 shell profile 使用。密钥不硬编码到技能代码。

## foxcode（OpenAI 兼容三方）—— 必需

FOX_CODE_API_KEY=sk-xxxx
# FOX_CODE_BASE_URL=https://dm-fox.rjj.cc/codex/v1   # 默认值，一般无需改
# FOX_CODE_DEFAULT_MODEL=gpt-image-2                 # 默认模型

## volcengine（火山引擎 arkcli）—— 可选

# 登录（SSO，推荐）：
#   arkcli auth login volc-sso
# 默认模型（可选覆盖）：
# ARK_DEFAULT_MODEL=doubao-seedream-4-5-251128

## bailian（千问百炼 bl）—— 可选

# 登录（与 bailian-cli 技能共享）：
#   bl auth
# 模型通过 --model 或渠道默认值指定（qwen-image-3.0）
